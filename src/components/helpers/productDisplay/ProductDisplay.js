import React from 'react'


/* //todo
  - Recieves props for state of displays (if state is empty then default is showing genres and everything)
  - Creates Header "{genre}"
  - Creates display of image, name, price, and add to cart button for 2 or 4 items depending on request
  - Creates an arrow to go to rest of genre games off of screen
*/


function ProductDisplay() {
  return (
    <div>
      ProductDisplay helper
    </div>
  )
}

export default ProductDisplay
